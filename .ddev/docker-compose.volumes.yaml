# Mount external SSD for files directory
services:
  web:
    volumes:
      # Mount the external SSD files directory directly (no symlink needed on host)
      - /Volumes/SCHNITZRAUM/projekte/site.kraiburg-tpe.com/live/pdb/assets:/var/www/html/assets:cached
